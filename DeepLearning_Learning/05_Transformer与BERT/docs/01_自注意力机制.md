# 自注意力机制

## 1. 注意力机制概述

### 1.1 从Seq2Seq到注意力

```python
import torch
import torch.nn as nn
import numpy as np
import math

def explain_attention():
    print("注意力机制的演进:")
    print("  1. Seq2Seq: 固定长度上下文向量")
    print("  2. Attention: 动态关注输入的不同部分")
    print("  3. Self-Attention: 序列内部元素相互关注")

explain_attention()
```

## 2. 自注意力机制原理

### 2.1 Query、Key、Value

```python
class SelfAttention(nn.Module):
    def __init__(self, embed_size, heads):
        super().__init__()
        
        self.embed_size = embed_size
        self.heads = heads
        self.head_dim = embed_size // heads
        
        assert self.head_dim * heads == embed_size, "embed_size必须能被heads整除"
        
        self.queries = nn.Linear(embed_size, embed_size)
        self.keys = nn.Linear(embed_size, embed_size)
        self.values = nn.Linear(embed_size, embed_size)
        
        self.fc_out = nn.Linear(embed_size, embed_size)
    
    def forward(self, query, key, value, mask=None):
        N = query.shape[0]
        q_len, k_len, v_len = query.shape[1], key.shape[1], value.shape[1]
        
        Q = self.queries(query)
        K = self.keys(key)
        V = self.values(value)
        
        Q = Q.view(N, q_len, self.heads, self.head_dim).transpose(1, 2)
        K = K.view(N, k_len, self.heads, self.head_dim).transpose(1, 2)
        V = V.view(N, v_len, self.heads, self.head_dim).transpose(1, 2)
        
        energy = torch.matmul(Q, K.transpose(-2, -1))
        energy = energy / math.sqrt(self.head_dim)
        
        if mask is not None:
            energy = energy.masked_fill(mask == 0, float("-1e20"))
        
        attention = torch.softmax(energy, dim=-1)
        
        out = torch.matmul(attention, V)
        
        out = out.transpose(1, 2).contiguous().view(N, q_len, self.embed_size)
        
        out = self.fc_out(out)
        
        return out, attention

embed_size = 512
heads = 8
batch_size = 2
seq_len = 10

self_attn = SelfAttention(embed_size, heads)
x = torch.randn(batch_size, seq_len, embed_size)

output, attention_weights = self_attn(x, x, x)
print(f"输入形状: {x.shape}")
print(f"输出形状: {output.shape}")
print(f"注意力权重形状: {attention_weights.shape}")
```

### 2.2 自注意力计算过程详解

```python
def self_attention_step_by_step():
    print("\n自注意力计算步骤:")
    print("=" * 50)
    
    seq_len = 4
    embed_size = 8
    
    x = torch.randn(1, seq_len, embed_size)
    print(f"1. 输入序列 X: {x.shape}")
    
    W_q = nn.Linear(embed_size, embed_size)
    W_k = nn.Linear(embed_size, embed_size)
    W_v = nn.Linear(embed_size, embed_size)
    
    Q = W_q(x)
    K = W_k(x)
    V = W_v(x)
    print(f"\n2. 线性变换得到 Q, K, V: {Q.shape}")
    
    scores = torch.matmul(Q, K.transpose(-2, -1))
    print(f"\n3. 计算注意力分数 QK^T: {scores.shape}")
    
    d_k = embed_size
    scores = scores / math.sqrt(d_k)
    print(f"\n4. 缩放 (除以 sqrt(d_k)): {scores.shape}")
    
    attention_weights = torch.softmax(scores, dim=-1)
    print(f"\n5. Softmax归一化: {attention_weights.shape}")
    
    output = torch.matmul(attention_weights, V)
    print(f"\n6. 加权求和得到输出: {output.shape}")
    
    print("\n注意力权重示例:")
    print(attention_weights[0])

self_attention_step_by_step()
```

## 3. 多头注意力

### 3.1 多头注意力原理

```python
class MultiHeadAttention(nn.Module):
    def __init__(self, embed_size, num_heads, dropout=0.1):
        super().__init__()
        
        assert embed_size % num_heads == 0
        
        self.embed_size = embed_size
        self.num_heads = num_heads
        self.head_dim = embed_size // num_heads
        
        self.q_proj = nn.Linear(embed_size, embed_size)
        self.k_proj = nn.Linear(embed_size, embed_size)
        self.v_proj = nn.Linear(embed_size, embed_size)
        
        self.out_proj = nn.Linear(embed_size, embed_size)
        self.dropout = nn.Dropout(dropout)
    
    def forward(self, query, key, value, mask=None):
        batch_size = query.size(0)
        
        Q = self.q_proj(query)
        K = self.k_proj(key)
        V = self.v_proj(value)
        
        Q = Q.view(batch_size, -1, self.num_heads, self.head_dim).transpose(1, 2)
        K = K.view(batch_size, -1, self.num_heads, self.head_dim).transpose(1, 2)
        V = V.view(batch_size, -1, self.num_heads, self.head_dim).transpose(1, 2)
        
        scores = torch.matmul(Q, K.transpose(-2, -1)) / math.sqrt(self.head_dim)
        
        if mask is not None:
            scores = scores.masked_fill(mask.unsqueeze(1) == 0, float('-inf'))
        
        attn_weights = torch.softmax(scores, dim=-1)
        attn_weights = self.dropout(attn_weights)
        
        attn_output = torch.matmul(attn_weights, V)
        
        attn_output = attn_output.transpose(1, 2).contiguous().view(
            batch_size, -1, self.embed_size
        )
        
        output = self.out_proj(attn_output)
        
        return output, attn_weights

print("\n多头注意力:")
mha = MultiHeadAttention(embed_size=512, num_heads=8)
x = torch.randn(2, 10, 512)
output, weights = mha(x, x, x)
print(f"输入: {x.shape}")
print(f"输出: {output.shape}")
print(f"注意力权重: {weights.shape}")
```

### 3.2 多头注意力的优势

```python
def explain_multihead_benefits():
    print("\n多头注意力的优势:")
    print("  1. 多个子空间: 每个头关注不同的特征")
    print("  2. 并行计算: 多个头可以并行处理")
    print("  3. 丰富表示: 捕获不同类型的依赖关系")
    print("\n示例:")
    print("  - 头1: 关注语法关系")
    print("  - 头2: 关注语义关系")
    print("  - 头3: 关注位置关系")

explain_multihead_benefits()
```

## 4. 位置编码

### 4.1 正弦位置编码

```python
class PositionalEncoding(nn.Module):
    def __init__(self, embed_size, max_len=5000, dropout=0.1):
        super().__init__()
        
        self.dropout = nn.Dropout(dropout)
        
        pe = torch.zeros(max_len, embed_size)
        position = torch.arange(0, max_len, dtype=torch.float).unsqueeze(1)
        
        div_term = torch.exp(
            torch.arange(0, embed_size, 2).float() * (-math.log(10000.0) / embed_size)
        )
        
        pe[:, 0::2] = torch.sin(position * div_term)
        pe[:, 1::2] = torch.cos(position * div_term)
        
        pe = pe.unsqueeze(0)
        
        self.register_buffer('pe', pe)
    
    def forward(self, x):
        x = x + self.pe[:, :x.size(1), :]
        return self.dropout(x)

pos_enc = PositionalEncoding(embed_size=512, max_len=100)
x = torch.randn(2, 20, 512)
output = pos_enc(x)
print(f"\n位置编码:")
print(f"输入: {x.shape}")
print(f"输出: {output.shape}")
```

### 4.2 可学习位置编码

```python
class LearnablePositionalEncoding(nn.Module):
    def __init__(self, embed_size, max_len=512):
        super().__init__()
        
        self.pos_embedding = nn.Embedding(max_len, embed_size)
    
    def forward(self, x):
        batch_size, seq_len, _ = x.shape
        positions = torch.arange(seq_len, device=x.device).unsqueeze(0).expand(batch_size, -1)
        pos_enc = self.pos_embedding(positions)
        return x + pos_enc

print("\n位置编码类型:")
print("  1. 正弦编码: 固定，可外推")
print("  2. 可学习编码: 灵活，有长度限制")
print("  3. 相对位置编码: 关注相对位置关系")
```

## 5. 注意力掩码

### 5.1 Padding Mask

```python
def create_padding_mask(seq, pad_idx=0):
    return (seq != pad_idx).unsqueeze(1).unsqueeze(2)

seq = torch.tensor([[1, 2, 3, 0, 0], [4, 5, 6, 7, 8]])
mask = create_padding_mask(seq)

print("\nPadding Mask:")
print(f"序列:\n{seq}")
print(f"掩码形状: {mask.shape}")
print(f"掩码:\n{mask.squeeze()}")
```

### 5.2 Look-ahead Mask (Causal Mask)

```python
def create_causal_mask(size):
    mask = torch.triu(torch.ones(size, size), diagonal=1).bool()
    return ~mask

size = 5
causal_mask = create_causal_mask(size)

print("\n因果掩码 (Causal Mask):")
print(f"形状: {causal_mask.shape}")
print(f"掩码:\n{causal_mask.int()}")

print("\n掩码作用:")
print("  - 防止当前位置看到未来信息")
print("  - 用于自回归生成")
```

## 6. 注意力可视化

```python
def visualize_attention():
    print("\n注意力权重可视化示例:")
    
    words = ["The", "cat", "sat", "on", "the", "mat"]
    
    attention = torch.softmax(torch.randn(6, 6), dim=-1)
    
    print("\n词到词的注意力权重:")
    print("      ", end="")
    for w in words:
        print(f"{w:>6}", end="")
    print()
    
    for i, w in enumerate(words):
        print(f"{w:>6}", end="")
        for j in range(len(words)):
            print(f"{attention[i,j]:>6.2f}", end="")
        print()

visualize_attention()
```

## 7. 总结

| 概念 | 说明 |
|------|------|
| Query | 查询向量，表示当前关注点 |
| Key | 键向量，用于匹配查询 |
| Value | 值向量，实际的信息内容 |
| 缩放因子 | 1/√d_k，防止点积过大 |
| 多头 | 多个子空间并行计算 |
| 位置编码 | 为序列添加位置信息 |
| 掩码 | 处理变长序列和因果约束 |
